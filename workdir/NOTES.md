# Sample notes for eval task S2 (read_file).

The ReAct loop is: reason → act → observe, repeated until Final Answer or max-steps.
The tool registry maps names to (schema, handler) pairs.
Errors come back as observations, not crashes.
